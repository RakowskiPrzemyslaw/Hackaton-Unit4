import React, { Component } from 'react';
import { Header } from '../../utils/styledComponents';
// import { Header } from './Events_styles';

export default class Events extends Component {
  render() {
    return (
      <Header>Events</Header>
    );
  }
}
