import React, { Component } from 'react';
import { Header } from '../../utils/styledComponents';
// import { Header } from './Workshops_styles';

export default class Workshops extends Component {
  render() {
    return (
      <Header>Workshops</Header>
    );
  }
}
